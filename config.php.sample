<?php 
	// Require the config file of the project in here if necessary 
	/* This is the configuration file, in the future may need to modify it to 
	* fetch from a file in the project
	*/  
	class DB_CONFIG{
		var $host = '';
		var	$user = ''; 
		var	$pass = '';
		var	$db   = '';
		var $versions_dir = ""; 
	}
?>